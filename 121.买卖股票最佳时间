class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        max_prices = 0
        ans = prices[0]
        for p in prices:
            max_prices = max(max_prices,p-ans) 
            ans = min(ans,p)
        return max_prices
#2026-1-5复习
